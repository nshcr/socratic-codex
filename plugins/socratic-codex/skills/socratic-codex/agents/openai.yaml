interface:
  display_name: "Socratic Codex"
  short_description: "Gate drift, stalled diagnosis, and weak closure."
  default_prompt: "Use $socratic-codex to protect material boundaries, recover from evidence plateaus, and close this task with proportionate proof."
policy:
  allow_implicit_invocation: false
