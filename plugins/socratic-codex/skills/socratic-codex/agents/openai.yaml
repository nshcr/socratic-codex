interface:
  display_name: "Socratic Codex"
  short_description: "Prevent goal drift in long tasks"
  default_prompt: "Use $socratic-codex to keep this long task aligned."
policy:
  allow_implicit_invocation: true
